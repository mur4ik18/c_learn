#include <malloc.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    // malloc(); выделяет в куче место
    // malloc(size*sizeof(int))
    // calloc(); выделяет место в куче и забивает его 0
    // free(); чистит память
    
    return 0;
}